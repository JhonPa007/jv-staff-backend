const { Client } = require('pg'); const client = new Client('postgresql://postgres:JbCjfwFkrmmbuQdkFpCWGvNEbmqCUldc@gondola.proxy.rlwy.net:17823/railway'); client.connect().then(async () => { try { const tables = ['empleado_bonos', 'empleado_penalidades', 'gastos']; for (const table of tables) { const res = await client.query('SELECT column_name FROM information_schema.columns WHERE table_name = \', [table]); console.log(table + ': ' + res.rows.map(r => r.column_name).join(', ')); } } catch (err) { console.error(err); } finally { client.end(); } });
